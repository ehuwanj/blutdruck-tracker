/// Severity tier of a rule-based insight. Drives chip color in the
/// statistics insights card. `warning` uses `caution`/`warn` palette tokens;
/// never the `alert` color.
enum InsightSeverity { info, warning, neutral }
